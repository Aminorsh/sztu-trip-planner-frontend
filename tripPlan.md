## 一、页面清单（不含后台管理）

下面是前端应包含的主要页面：


| 序号 | 页面名称               | 路由 /示例路径                                   | 备注 /是否弹窗式可能 | 主要用途                                     |
| ---- | ---------------------- | ------------------------------------------------ | -------------------- | -------------------------------------------- |
| 1    | 首页 / Landing         | `/`                                              | 正常页面             | 吸引新访客 /展示产品特色 /注册 /登录入口     |
| 2    | 登录 / 注册 / 找回密码 | `/login`, `/register`, `/forgot-password`        | 正常页面             | 用户账号认证流程                             |
| 3    | 仪表板 / 我的行程列表  | `/dashboard` 或 `/trips`                         | 正常页面             | 展示用户已有行程 /新建行程入口               |
| 4    | 行程详情 / 编辑        | `/trips/:tripId`                                 | 正常页面             | 核心编辑 /路径 /地点 /打卡管理               |
| 5    | 地点搜索 /添加         | 通常为弹窗或子路由，如`/trips/:tripId/add-place` | 弹窗 /浮层           | 用户搜索 /浏览可选地点 /添加到行程           |
| 6    | 地点详情               | `/places/:placeId`                               | 正常页面             | 展示单个打卡点详细信息 /加入行程入口         |
| -    | 分享 / 公开展示        | `/share/:shareId`                                | 正常页面（只读）     | 他人查看这个行程 /路线 /足迹 /地点信息       |
| 8    | 导出 / 下载界面        | `/trips/:tripId/export` 或作为弹窗               | 弹窗 /子页面         | 用户选择导出格式 /预览 /下载 /状态显示       |
| -    | 通知 / 消息            | `/notifications`                                 | 正常页面             | 展示邀请 /协作变更 /导出完成等通知给用户     |
| 10   | 用户资料 /设置         | `/profile` 或 `/settings`                        | 正常页面             | 修改用户昵称 /头像 /密码 /通知偏好 /隐私设置 |

这个就是一个合理的前端页面总表（前提是不做后台管理界面，由后台管理模块单独存在或由管理员工具）。你可以根据优先级将部分导出 /通知 /地点详情页面甚至做为弹窗简化。

---

## 二、每个页面应包含的功能模块 /要点

下面详细列出每个页面中应包含的功能或子模块，供设计 /前端开发参考。

### 1. 首页 / Landing

**主要功能 /模块：**

- 顶部导航栏（Logo、菜单、登录 /注册按钮）
- 横幅 / Hero 区（大视觉 /标语 /主要 Call-to-Action）
- 功能 /亮点展示（图标 + 描述）
- 示例 /行程展示 /案例卡片
- 用户评价 /媒体 /信任标识
- Footer（关于 /隐私 /帮助 /联系方式 /社交）

**注意点：**

- 登录 /注册按钮要显眼
- 示例行程卡片可点击进入分享 /公开页面
- 对访客应有吸引力，同时让已登录用户快速进入仪表板

---

### 2. 登录 / 注册 /忘记密码页面

**主要功能 /模块：**

- 表单输入（邮箱 /用户名 /密码 /确认 /验证码等）
- 验证 /输入校验 /错误提示
- 提交按钮 /loading 状态
- 社交登录 /第三方登录按钮
- 辅助链接（切换登录 /注册 /忘记密码）

**注意点：**

- 表单控件要有焦点 /验证 /错误反馈
- 注册成功后跳转仪表板或首个行程创建页
- 忘记密码流程：提交邮箱 → 邮件重设 → 验证新密码

---

### 3. 仪表板 / 我的行程列表页面

**主要功能 /模块：**

- 顶部导航 /用户菜单
- 搜索 /过滤 /排序（按日期 /地点 /状态）
- 行程卡片 /列表展示，每个卡片包含：封面图 /标题 /日期范围 /天数 /进入 /删除 /编辑 /分享按钮
- “新建行程”按钮
- 空状态提示（若无行程时引导）
- 分页 /无限滚动 /加载更多

---

### 4. 行程详情 / 编辑页面（最核心页面）

**主要功能 /模块：**

- 顶部工具栏：行程标题 /保存状态 /分享 /导出 /协作状态 /设置按钮
- 左侧：天数切换 /行程项列表 /拖拽排序 /添加按钮
- 中间：地图视图 + 标记 +路线 +足迹展示 +地图工具按钮
- 右侧：当选中某项时显示属性 / 修改 /删除 /重排 /打卡按钮 /备注 /优先级 /交通时间 /距离显示
- 优化 / 重排整体按钮
- 实时协作 /变动广播处理
- 打卡 /记录 /展示足迹轨迹
- 导出 /分享入口
- 底部状态栏 /反馈 /提示

**中间部分填充内容说明**
在你页面中，中间那一块空白（地图旁边 / 地图下面留白），我**建议**那块可以填的内容有：

* **路线 & 足迹展示**：地图上显示线路连接点 + 高亮行程路径
* **行程描述 / 概览**：显示本天的总览、天气、交通建议、今日摘要
* **照片 / 回忆 / 相册**：当天行程拍摄的照片缩略展示
* **导航 / 切换日图 / 路线图切换按钮**：比如切换 “地图视图 / 列表视图 / 路线视图”
* **交通时间 / 距离汇总**：展示视图下各点之间的交通时间、距离总和

这样那块就不会空白，而是有实质内容与交互。



* 地图容器目前是 placeholder 背景，你后续需要接入真实地图库（如 Mapbox、Leaflet、Google Maps）并绑定 `ref="mapContainer"` 进行初始化
* 日历里的事件数据、左侧行程项数据、摘要区的数据都用的是 mock，你后续需要接入后端 API 或本地状态机管理

---

### 5. 地点搜索 / 添加（弹窗或子界面）

**主要功能 /模块：**

- 搜索输入框 + 自动补全 /防抖
- 筛选 /排序（如按类别 /距离 /评分 /热点）
- 搜索结果列表（地点卡片，展示名称 /地址 /图片 /简介 /是否已添加）
- 点击结果查看简要详情 /展开更多
- “加入行程”按钮 +选择天 /插入位置 /时间
- 关闭 /取消按钮

---

### 6. 地点详情页面

**主要功能 /模块：**

- 地点封面图片 /图片轮播 /缩略图
- 名称 /地址 /类别 /评分
- 地图小视图 /定位
- 简介 /亮点 /历史 /特色
- 营业 /开放时间 /票价 /须知 /费用信息表
- “加入行程”按钮 +选日 /时段
- 返回 /导航 /关闭按钮

---

### 7. 分享 /公开展示页面（只读）

**主要功能 /模块：**

- 顶部：行程标题 /作者 /日期 /封面图 /分享 /复制链接按钮
- 天数切换 /行程日程表（只读）
- 地图 /路线 /实际足迹展示
- 地点点击弹出只读详情（名称 /简介 /开放时间 /图片）
- 导出 /下载按钮（如果分享权限允许）

---

### 8. 导出 / 下载界面 /弹窗

**主要功能 /模块：**

- 导出格式选择（PDF /图片 /KML /ICS 等）
- 附加选项设置（是否包含足迹 /标注 /地图风格 /封面 /字体等）
- “导出”按钮 /loading /禁用状态
- 导出状态 /进度 /错误提示 /重试按钮
- 下载链接 /导出预览缩略图

---

### 9. 通知 / 消息页面

**主要功能 /模块：**

- 通知列表（每条含图标 /时间 /标题 /内容 /操作按钮）
- 点击跳转 /处理（如“接受邀请” /“查看行程”）
- 批量标记已读 /删除功能
- 筛选 /排序（所有 /未读 /邀请 /系统）
- 实时通知插入（WebSocket）

---

### 10. 用户资料 /设置页面

**主要功能 /模块：**

- 个人资料编辑：头像 /用户名 /邮箱 /简介等
- 密码修改（旧密码 /新密码 /确认）
- 通知 /推送偏好设置
- 隐私 /分享默认设置
- 保存 /取消按钮 +反馈
- 注销 /删除账号按钮

---

## 三、页面数量 &开发优先级建议

- 总共 **10 个页面** 如上（可根据需求精简 /先做核心模块）
- 开发建议分阶段上线：
  - **优先阶段（MVP）**：登录 /注册、仪表板、行程详情 / 编辑、地点搜索 / 添加、地点详情、分享 /只读页面
  - **中后阶段**：导出 /下载界面、通知 /消息页面、用户资料 /设置

## 下一步建议 /后端对接前的准备

接下来，你可以做以下工作：

1. **接口文档 /后端对接**
   与后端一起明确每个 API 路径、请求参数、响应格式。这很关键，前端服务调用必须与后端一致。
2. **填充页面骨架 /UI 结构**
   在各 `views/` 下填入每页面的结构、基本 DOM /组件组合 /布局。
3. **路由守卫 /权限控制**
   在 Router 中加入验证逻辑（如未登录重定向登录页、访问无权限行程提示等）。
4. **页面数据获取 & 与 store /服务的交互**
   在 `beforeMount` /`onMounted` /`setup` 中调用服务获取数据，然后写入 store /局部状态。
5. **样式 / 主题 /响应式设计**
   设计各页面 /组件样式，写 CSS /SCSS /变量 /mixins，保证在不同分辨率下布局合理。
6. **错误 / loading /空状态处理**
   页面加载期间显示 loading、接口失败显示错误提示、无数据时显示空状态 UI。
7. **地图组件 /路线渲染 /打卡展示**
   对于行程详情页面，调入地图组件，展示标记 /连线 /轨迹。
8. **导出 /分享 /通知 /协作逻辑**
   依次开发导出页面、分享页面、通知模块、协作状态同步等。
9. **测试 /调试 /优化**
   不断在页面间切换、模拟错误、调试网络调用、优化性能、目录结构复查等。
