## 2025.10.28

## 总结

以下是截至目前为止，我们前端部分已经完成 / 初步实现的各个页面 / 组件、它们的功能模块和状态说明，以及我们遇到的问题、风格设计原则和下一步建议。

---

## 已完成页面 / 组件 功能模块 / 状态说明

下面按照页面顺序整理：

#### **Home.vue（首页）**

* **功能 / 模块**：顶部导航栏、Hero 区（图片轮播 + 叠层遮罩效果 + 文本 + CTA 按钮）、功能亮点板块、热门行程示例板块、用户心声 / 评价板块、页脚 Footer
* **状态 /说明**：
  * 静态版本已实现，使用假数据填充。
  * Hero 区实现了 3 张图片轮播切换，并加了遮罩渐变叠层增强视觉层次。
  * 各功能 / 示例 / 评价板块布局完成，卡片样式、图形装饰、圆角按钮、阴影、配色等都经过多次迭代，从最初的较为单调样式逐渐丰富起来。
  * 导航、按钮、文字配色、间距布局在多个版本中不断调整以提升视觉效果与协调性。

#### **登录 / 注册 / 忘记密码页面**

* **功能 / 模块**：表单输入（邮箱 / 用户名 / 密码 / 确认 / 验证码 等）、输入验证 / 错误提示、提交 / loading 状态、辅助切换链接（登录 ↔ 注册 ↔ 忘记密码）、社交登录按钮展示
* **状态 /说明**：
  * 已完成页面布局与基本交互（本地验证 / 提交占位逻辑）。
  * 样式部分多次迭代：使表单整体居中、按钮分布合理、背景图 + 遮罩效果增强层次、按钮与字体风格柔和统一。
  * 正确处理了“忘记密码 / 注册 / 登录”辅助链接的排列方式、状态显示、颜色淡化等细节。

#### **Dashboard.vue（仪表板 / 行程列表）**

* **功能 / 模块**：顶部导航与用户菜单、新建行程按钮、搜索 / 过滤输入、行程卡片展示（封面、标题、日期 / 天数信息）、卡片上的进入／编辑／删除／分享按钮、空状态提示、侧边栏 / 右侧用户信息栏布局
* **状态 /说明**：
  * 静态版本已搭建，假数据填充多个示例行程。
  * 卡片样式、阴影、圆角、叠层装饰、响应式列布局、滚动容器均已实现。
  * 侧边栏与右侧用户栏布局已就绪，用占位图片与文字模拟。
  * 搜索输入、按钮点击跳转等事件绑定已做基础 stub（后续要接真实逻辑）。

#### **TripDetail.vue（行程详情 / 编辑）**

* **功能 / 模块**：

  * 顶部工具栏（保存 / 分享 / 设置 / 添加地点 / 导出）
  * 左侧：天数切换按钮 + 行程项列表（可拖拽排序）
  * 日历视图（展示当天行程分布）
  * 中间：地图区（占位背景 + 地图工具按钮） + 本日概览摘要（总距离 / 总时间 / 路线照片等）
  * 右侧：当选中行程项时可编辑属性（名称 / 时间 / 备注 / 优先级） / 删除 / 保存 / 打卡按钮
  * 底部状态栏 + “优化路线”按钮
  * 弹窗调用：PlaceSearch 弹窗、ExportPanel 弹窗
* **状态 /说明**：

  * 布局结构完整，静态原型版本可运行。
  * 假数据填充，日历插件（FullCalendar）已集成、事件渲染已配置。
  * 地图区目前用背景图占位，地图工具按钮布局已放。
  * PlaceSearch 弹窗与 ExportPanel 弹窗已正确挂载与控制逻辑（visible 控制）
  * 样式方面已有渐变叠层、遮罩层、圆角按钮、阴影效果，整体风格已趋统一。
* 中间部分填充内容说明**在你页面中，中间那一块空白（地图旁边 / 地图下面留白），我**建议**那块可以填的内容有：

  * **路线 & 足迹展示**：地图上显示线路连接点 + 高亮行程路径
  * **行程描述 / 概览**：显示本天的总览、天气、交通建议、今日摘要
  * **照片 / 回忆 / 相册**：当天行程拍摄的照片缩略展示
  * **导航 / 切换日图 / 路线图切换按钮**：比如切换 “地图视图 / 列表视图 / 路线视图”
  * **交通时间 / 距离汇总**：展示视图下各点之间的交通时间、距离总和

- 这样那块就不会空白，而是有实质内容与交互。

* 地图容器目前是 placeholder 背景，你后续需要接入真实地图库（如 Mapbox、Leaflet、Google Maps）并绑定 `ref="mapContainer"` 进行初始化
* 日历里的事件数据、左侧行程项数据、摘要区的数据都用的是 mock，你后续需要接入后端 API 或本地状态机管理

#### **PlaceSearch.vue（地点搜索 / 弹窗 / 子界面）**

* **功能 / 模块**：搜索输入框 + 防抖处理、类别筛选、排序选项、地点卡片列表（名称 / 地址 / 简介 / 图片 / “加入行程”按钮 / “详情”按钮）、展开 “详情” 显示更多信息（如评分 / 距离 / 开放时间等）、加入行程发出事件、关闭弹窗
* **状态 /说明**：
  * 静态版本已实现搜索、筛选、排序输入控件与其响应。
  * 卡片布局、展开/收起逻辑、加入按钮触发父组件 `@place-added` 事件逻辑已实现。
  * 点击 “详情” 的跳转逻辑我们讨论中已设计为路由跳转至 `PlaceDetail.vue`。
  * 默认图片占位、样式统一、弹窗遮罩 / 过渡效果均已实现。

#### **PlaceDetail.vue（地点详情页 / 组件）**

* **功能 / 模块**：地点封面图 / 图片轮播 / 缩略图展示、名称 / 地址 / 类别 / 评分、地图小视图 / 定位、简介 / 亮点 / 历史 / 特色文字内容、营业 / 开放时间 / 票价 / 须知 / 费用等信息表格、加入行程按钮 + 选择日期 / 时段、返回 / 关闭 / 导航按钮
* **状态 /说明**：
  * 目前尚未在总结中写出，但在后续我们确立了设计思路，并可实现静态原型。
  * 我们规划在 PlaceSearch 中 “详情” 按钮跳转至此页面，且在 PlaceDetail 页面也提供 “加入行程” 操作（发事件 / 路由返回 /向 TripDetail 注入）
  * 在样式方面，我们已讨论要用两张图片（一个圆形、一个矩形）叠加、按钮圆角、表单 / 信息模块左一右二布局、尽量减少空白、配色延续整体风格。

#### **ExportPanel.vue（行程导出弹窗组件）**

* **功能 / 模块**：导出格式选择（PDF / 图片 / KML / ICS）、附加选项（是否包含足迹 / 地点标注 / 地图底图 / 封面页等）、导出按钮 / loading / 禁用状态 / 状态文字提示、成功后显示下载链接 / 下载按钮 / 重试按钮、预览缩略图展示
* **状态 /说明**：
  * 静态版本已实现，采用假导出 API 模拟异步导出流程。
  * 支持 `v-model:visible` 与 `modelValue / update:modelValue` 双向绑定方式。
  * 弹窗遮罩、样式圆角、panel 布局、过渡动画、按钮样式、状态显示均已完成。

#### **Profile.vue（用户资料 / 设置页）**

* **功能 / 模块**：
  * 个人资料编辑（头像上传 / 用户名 / 邮箱 / 简介）
  * 密码修改（旧密码 / 新密码 / 确认密码）
  * 通知 / 推送偏好设置开关
  * 隐私 / 分享默认设置（例如默认行程分享状态）
  * 保存 / 取消按钮 + 提示反馈
  * 注销 / 删除账号按钮 + 确认操作
* **状态 /说明**：
  * 初稿版本已实现整体结构与表单控件、验证提示、按钮逻辑、样式风格统一。
  * 上传头像使用 `el-upload` + Base64 处理的示例逻辑。
  * 密码修改 / 偏好 / 隐私设置模块分开，按钮触发模拟保存 + 显示提示。
  * 注销 / 删除账号弹窗确认逻辑也写出示例。
  * 样式保持圆角、卡片、阴影、浅色背景、主点色调等风格一致性。

---

## 遇到的关键问题与解决方案

在这一路开发过程中，我们遇到了一些问题／挑战，下面是主要几个，以及我们是如何解决或规避的。


| 问题 /挑战                                       | 说明                                                                                                                      | 解决 /规避方式                                                                                                                         |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **Vite + Vue3 中不能用`require(...)`引静态图片** | 有时候在代码里写`require('@/assets/xxx.jpg')`会报错 “require is not defined”                                            | 我们统一使用 ESM 导入方式：`import img1 from '@/assets/images/...'`或者把图放`public`目录然后用`/images/...`路径引用                   |
| **v-model 绑定命名不一致导致弹窗不显示**         | 我们一开始在父组件用了`v-model:visible="exportVisible"`，而子组件用的是`modelValue`/`update:modelValue`接口，导致绑定失效 | 我们统一改用`v-model="exportVisible"`，匹配子组件的`modelValue`接口。这样点击 “导出” 按钮将`exportVisible = true`会正确弹出          |
| **布局 / 样式不一致 / 组件拥挤 / 颜色搭配**      | 多次你提出 “布局太挤”、“按钮不好看”、“颜色太单调”、“背景图空白”等意见                                             | 我们逐步在每个页面中调整：加遮罩、渐变叠层、图形叠加、圆角按钮、阴影、配色统一（主紫 / 主蓝点缀色）、渐变叠层背景、控制明度 / 对比度等 |
| **日历高度 / 滚动 / 空白区处理**                 | FullCalendar 默认可能预留额外空白 / 出现滚动条的问题                                                                      | 我们通过 CSS 强制设定`.fc-scrollgrid`、`.fc-scroller`、`.fc-daygrid-body`的`height: auto`/`overflow: hidden`等，去除多余区域。         |
| **弹窗 / teleport 层级 / 遮罩冲突**              | 弹窗可能被页面其他元素遮挡、在某些布局内不显示                                                                            | 使用`teleport to="body"`保证弹窗挂载在 body 顶层，并给`.export-overlay`设很高`z-index`，遮罩层点击`.self`关闭                          |

---

## 整体风格 / 设计要点

在开发过程中，我们坚持以下风格和设计原则：

* **浅色系 + 点缀色**：整体背景浅色，中间区域白 / 浅灰 / 透明覆盖，使用紫蓝、粉红、渐变等作为点缀色（按钮、叠层、图形装饰）。
* **圆角 / 阴影 / 渐变叠层**：按钮多圆角，卡片、弹窗带阴影，叠层背景使用渐变或图形叠层以增加层次感。
* **清晰的层级 / 层次感**：背景图 + 遮罩 + 内容层，模块之间用内边距 / 间距区分，多版块色块或背景变换区分不同板块。
* **动效 / 交互提示**：按钮按下有缩放效果，卡片 hover 有微位移，弹窗 fade 过渡，轮播渐变过渡，折叠 / 展开有动画等。
* **统一字体 / 字号 /色调**：统一使用现代无衬线字体（如 Poppins / Nunito Sans），字号体系一致，文字颜色深浅协调。

## 下一步建议

1. **实现 PlaceDetail.vue 静态原型 / 路由跳转**

* 用假数据填充 PlaceDetail 页面，包含封面图、简介、信息表格、地图小视图、加入行程按钮、返回 / 关闭按钮
* 在 PlaceSearch 的 “详情” 按钮上调用 `this.$router.push({ name: 'PlaceDetail', params: { placeId: place.id } })`
* 在 PlaceDetail 页面通过路由参数 `placeId` 获取对应地点数据展示

2. **加入行程逻辑**

* 在 PlaceDetail 页面点击 “加入行程” 时，发出事件或调用接口，将地点加入当前行程（或跳回 TripDetail 并注入）
* 支持选择哪个天 / 插入到哪个时段 / 起始时间等参数

3. **与后端接口对接 / 真数据支持**

* 地点详情页从后端获取地点详情（名称 / 地址 / 图片 / 时间 /票价 /须知等）
* PlaceSearch 搜索结果、分页、加载更多、排序 / 筛选逻辑用真实 API 替换模拟数据
* 导出 / 行程操作 / 用户资料 / 保存逻辑统一接入后端

4. **样式微调 / 响应式支持**

* 地点详情页在手机 / 平板上的响应式布局（图片区域、说明区、加入按钮等重排）
* 图片加载占位、懒加载、轻量化处理
* 滚动容器、长内容折叠/展开控制

5. **状态管理 / 全局通信**

* 在多个页面 / 组件间（TripDetail / PlaceDetail / PlaceSearch）共享行程数据可以考虑使用 Pinia / Vuex
* 路由导航守卫 / 权限控制（未登录用户不允许查看行程 / 加入地点等）

6. **性能 / 技术优化**

* 异步组件 / 路由懒加载
* 缓存 / 请求去重 / 错误重试策略
* 图片 / 图标资源优化
